# 🌊 深海花园 — 项目架构与设计文档

## 一、项目概述

**深海花园** 是一款针对某款花园经营类手机游戏的 **PC 端自动化操作脚本集**。它通过截图识别 + 模板匹配 + 鼠标模拟的方式，代替人工完成种花、浇水、收获、加速、铲除、订单处理等一系列重复操作。

项目基于 **纯客户端视觉自动化**，不涉及 Hook、抓包、内存修改等侵入式手段，全程通过图像识别驱动鼠标操作，安全可靠。

---

## 二、技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| **截图引擎** | [MSS](https://github.com/BoboTiG/python-mss) | 毫秒级全屏截图，返回 numpy 数组 |
| **图像识别** | [OpenCV](https://opencv.org/) (cv2.matchTemplate) | 模板匹配，定位屏幕上的目标元素 |
| **图像处理** | numpy | 像素数组操作、坐标计算 |
| **鼠标模拟** | [pynput](https://pypi.org/project/pynput/) | 鼠标点击/长按/拖动、键盘 Esc 监听 |
| **拼音过滤** | [pypinyin](https://pypi.org/project/pypinyin/) | 花种选择时支持拼音首字母模糊过滤 |
| **窗口管理** | pywin32 + pygetwindow | 窗口操作（预留） |
| **图像加载** | Pillow | 图片格式支持（间接依赖） |

> 依赖列表见 `requirements.txt`，核心依赖仅 **6 个**，轻量且无 GPU 需求。

---

## 三、项目结构

```
深海花园/
├── 任务/                          # 操作脚本（按编号排列）
│   ├── 01-种花.py                 # 种植花朵
│   ├── 02-浇水.py                 # 浇水
│   ├── 03-收获.py                 # 收获花朵
│   ├── 04-加速.py                 # 加速生长
│   ├── 05-铲除.py                 # 铲除花朵
│   ├── 06-顾客订单.py             # 处理顾客订单
│   ├── 07-居民订单.py             # 提交居民订单
│   ├── 07-组团订单.py             # 组团订单处理（开发中）
│   ├── 08-偷花.py                 # 偷花摘取
│   └── 09-种花并浇水.py           # 组合脚本（种花+浇水）
│
├── 工具/                          # 核心工具模块
│   ├── 图像识别.py                # MSS 截图 + OpenCV 模板匹配引擎
│   ├── 读取配置.py                # 全局配置读取 + 坐标偏移计算
│   ├── 日志打印.py                # 同步/异步可配置的日志打印模块
│   └── 鼠标单击间隔测试.py        # 辅助测试工具
│
├── 素材/                          # 模板图片（与脚本编号对应）
│   ├── 01-种花模板/
│   │   ├── 模板/                  # 花种图标（如 向日葵.png）
│   │   ├── 空地/                  # 空土地截图
│   │   └── 其他素材/              # 种植界面入口按钮、翻页按钮
│   ├── 02-浇水/
│   │   ├── 浇水按钮.png
│   │   ├── 模板/                  # 缺水状态的花朵截图
│   │   └── 浇水排除/              # 排除模板（已浇过的花）
│   ├── 03-收获/
│   │   ├── 模板/                  # 成熟花朵截图（多种花）
│   │   └── 按钮/                  # 收获按钮.png
│   ├── 04-加速/
│   │   ├── 按钮/                  # 加速按钮.png、免费加速按钮.png
│   │   └── 模板/                  # 需加速的花朵截图
│   ├── 05-铲除/
│   │   └── 按钮/                  # 铲除按钮.png
│   ├── 07-居民订单/               # 牌子、界面、提交按钮、关闭按钮
│   ├── 08-顾客订单/               # 各类订单状态按钮（8-9 张模板）
│   ├── 08-偷花/
│   │   ├── 偷花按钮/              # 摘取按钮（多模板）
│   │   └── 摘取次数.png           # 用于过滤筛选
│   └── 新建文件夹/                # 未归类素材
│
├── 配置/
│   └── 设置.txt                   # 全局参数配置（间隔、偏移、上限等）
│
├── README.md                      # 项目介绍 + 使用说明
├── SKILL.md                       # 操作指南（更详细的版本）
├── requirements.txt               # Python 依赖
├── set                            # 环境脚本
└── 项目架构与设计文档.md          # 本文档
```

---

## 四、架构设计

### 4.1 三层架构

```
┌─────────────────────────────────────────────┐
│               任务层（Task）                   │
│   01-种花  02-浇水  03-收获  ...  08-偷花     │
│   每个模块 = 一个独立功能脚本                   │
│   可独立运行：python 任务/01-种花.py            │
└──────────────────────┬──────────────────────┘
                       │ 调用
┌──────────────────────▼──────────────────────┐
│               工具层（Utility）               │
│   ┌──────────┐ ┌──────────┐ ┌───────────┐   │
│   │ 图像识别 │ │ 读取配置 │ │  日志打印  │   │
│   │ (MSS+)  │ │ (ini解析)│ │ (sync/async)│   │
│   │ OpenCV) │ │          │ │           │   │
│   └──────────┘ └──────────┘ └───────────┘   │
└──────────────────────┬──────────────────────┘
                       │ 依赖
┌──────────────────────▼──────────────────────┐
│             系统层 / 运行时                    │
│    Python 3.x + MSS + OpenCV + pynput        │
│    Windows OS (pywin32/pygetwindow 适配)      │
└─────────────────────────────────────────────┘
```

### 4.2 图像识别流水线

每个脚本的核心工作流程：

```
MSS 屏幕截图（毫秒级）
    │
    ▼
OpenCV matchTemplate (TM_CCOEFF_NORMED)
    │  支持单模板 / 多模板匹配
    │  阈值设定（默认 0.7 ~ 0.8）
    ▼
坐标后处理
    │  去重合并（距离过近的坐标合并）
    │  Y→X 排序（按行优先排列）
    ▼
pynput 鼠标模拟
    │  点击 / 长按 / 拖动
    │  支持坐标偏移配置
    ▼
Esc 监听（守护线程）
    随时安全停止
```

### 4.3 两种操作模式

| 模式 | 原理 | 适用脚本 | 特点 |
|------|------|---------|------|
| **预录列表** | 先截图识别所有目标坐标 → 按列表顺序依次拖动到每个点 | 02-浇水、03-收获、04-加速 | 稳定，适合目标不会移动的场景 |
| **实时扫描** | 按住鼠标不放 → 拖动期间反复截图 + 识别剩余目标 | 01-种花、05-铲除 | 实时，适合目标会动态变化的场景（如种花时空地逐渐减少） |

### 4.4 安全机制

1. **Esc 键紧急停止** — 每个脚本启动时都会创建一个独立的后台守护线程，监听键盘 Esc 事件。按下后立即释放鼠标并进程退出。
2. **鼠标释放保障** — 停止回调中显式调用 `mouse.release(Button.left)`，防止鼠标卡住。
3. **异常捕获** — `KeyboardInterrupt` 捕获 Ctrl+C，确保优雅退出。

---

## 五、模块详解

### 5.1 工具层

#### `工具/图像识别.py`

核心视觉引擎，提供 3 个导出函数：

- **`屏幕截图()`** — 调用 MSS 截取主显示器全屏，返回 OpenCV BGR 格式的 numpy 数组。
- **`查找所有匹配(截图, 模板路径, 阈值)`** — 单模板匹配，返回所有匹配坐标，经过去重合并后排好序。
- **`查找所有匹配_多模板(截图, 模板文件夹, 阈值)`** — 扫描文件夹内所有 PNG 分别匹配，汇总结果并去重。
- **`查找单个匹配(截图, 模板路径, 阈值)`** — 找最佳匹配，只返回置信度最高的一个坐标（或 None）。

内部算法：

```python
# 模板匹配核心（仅一行）
cv2.matchTemplate(截图, 模板, cv2.TM_CCOEFF_NORMED)
# 阈值筛选
np.where(结果 >= 阈值)
# 坐标转换：从左上角 → 中心点
中心 = (pt[0] + 模板宽//2, pt[1] + 模板高//2)
# 去重策略：如果两个点的 X 或 Y 差距小于模板尺寸一半，视为同一个
```

#### `工具/读取配置.py`

- 惰性读取 `配置/设置.txt`，首次调用后缓存。
- 支持整数、浮点数、字符串三种类型解析。
- 提供 `计算偏移位置(x, y)` 函数，根据配置的偏移模式和像素量计算最终点击位置。

#### `工具/日志打印.py`

- 支持同步/异步两种模式（由 `USE_ASYNC_LOGGING` 控制）。
- 异步模式下通过后台线程 + 队列写日志，减少 I/O 对主流程的干扰。
- 默认为同步模式（False），可切换。

#### `工具/鼠标单击间隔测试.py`

- 独立的辅助测试工具，用于测量鼠标单击间隔，帮助调试操作速度。

### 5.2 农田操作类任务

| 脚本 | 功能 | 算法描述 |
|------|------|---------|
| **01-种花** | 种植花朵 | 交互式选择花种（支持拼音首字母过滤）→ 按住花种 → 实时扫描空地 → 逐块拖动种植 |
| **02-浇水** | 浇花 | 扫描缺水花 → 点击第一朵 → 按住浇水按钮 → 实时扫描拖动，支持排除模板（跳过已浇） |
| **03-收获** | 收获花朵 | 预扫描成熟花列表 → 点击第一朵 → 按住收获按钮 → 按列表依次拖动 |
| **04-加速** | 加速生长 | 类似收获，找需加速的花 → 可选免费加速 → 按住加速按钮拖动 |
| **05-铲除** | 铲除花朵 | 类似种花的实时扫描模式，找花 → 点第一朵 → 按住铲除按钮拖动 |

**关键交互**：01-种花.py 是唯一一个交互式脚本（需用户在终端选择花种），使用 `pypinyin` 实现拼音首字母过滤（如输入 `xrk` → 匹配"向日葵"）。

### 5.3 任务类脚本

| 脚本 | 功能 | 算法描述 |
|------|------|---------|
| **06-顾客订单** | 处理顾客订单 | 三种订单状态（已制作好/缺货/可制作），先统计顾客数 → 逐个状态判断 → 执行对应操作链 |
| **07-居民订单** | 提交居民订单 | 找入口牌子 → 进入界面 → 连点提交按钮 → 关闭界面，支持重试循环 |
| **07-组团订单** | 组团订单 | 开发中，识别组团进度 → 未满换人 / 已满提交 |
| **08-偷花** | 多模板批量摘取 | 一次扫描所有摘取按钮（多模板）→ 按 Y 坐标过滤（在摘取次数区域上方）→ 逐个点击 |

**订单流程（06-顾客订单）示例**：

```
统计顾客数（3 种状态模板计数）
  ↓
逐个处理订单：
  → 识别到"已制作好" → 交付（交付确认 → 交付 → 关闭）
  → 识别到"不可制作" → 缺货处理（暂时没货）
  → 识别到"可制作" → 制作流程（前往制作 → 制作 → 交付确认 → 交付 → 关闭）
```

---

## 六、配置系统

`配置/设置.txt` 是唯一的全局配置文件，TXT 格式键值对，支持 `#` 注释。

| 配置项 | 默认值 | 用途 |
|--------|--------|------|
| 拖动间隔 | 0.1s | 拖动到每个点之间的停顿时间 |
| 点击偏移 | 中心 | 点击坐标偏移方向（中心/左上角/右下角/下端） |
| 偏移像素 | 15 | 偏移像素量 |
| 最大上限 | 50 | 每次最多处理的目标数（0=不限制） |
| 居民订单提交尝试次数 | 8 | 居民订单每轮最多提交数 |
| 居民订单重试等待秒 | 60 | 找不到按钮时的重试等待时间 |
| 居民订单最大循环次数 | 20 | 居民订单流程最大循环轮数 |
| 种花最大翻页次数 | 5 | 种花翻页找花种的最大页数 |

---

## 七、数据流

以 **02-浇水.py** 为例，展示一次典型的完整数据流：

```
┌─────────┐    ┌──────────────┐    ┌─────────┐    ┌──────────────┐
│ 用户执行  │───▶│  启动守护线程  │    │  截图识图 │    │  鼠标模拟     │
│ python   │    │  (监听 Esc)  │    │          │    │              │
│ 02-浇水  │    └──────────────┘    │          │    │              │
└─────────┘                        │          │    │              │
         │                         │          │    │              │
         ▼                         ▼          │    ▼              │
    ┌──────────┐            ┌─────────────┐   │  ┌────────────┐  │
    │ 读取配置  │            │ MSS 截图     │   │  │ 点击第一朵花│  │
    │ (设置.txt)│            │ ↓           │   │  │ → 等待弹窗  │  │
    └──────────┘            │ OpenCV 匹配  │   │  └────────────┘  │
                            │ (多模板)     │   │         │        │
                            │ ↓           │   │         ▼        │
                            │ 去重 + 排序  │   │  ┌────────────┐  │
                            │ ↓           │   │  │ 找浇水按钮  │  │
                            │ 坐标列表     │   │  │ → 长按      │  │
                            └─────────────┘   │  └────────────┘  │
                                    │         │         │        │
                                    └─────────┘         ▼        │
                                                   ┌────────────┐ │
                                                   │ 循环:       │ │
                                                   │ 截图→匹配→  │ │
                                                   │ 拖动到最近花 │ │
                                                   │ 直到全部浇完 │ │
                                                   └────────────┘ │
                                                         │        │
                                                         ▼        │
                                                   ┌────────────┐  │
                                                   │ 释放鼠标    │  │
                                                   │ 打印完成    │  │
                                                   └────────────┘  │
```

---

## 八、设计亮点

1. **全中文标识符** — 函数名、变量名、注释全部使用中文，降低理解门槛，方便非程序员用户修改。
2. **单文件可运行** — 每个任务脚本都是独立的 `__main__` 入口，直接 `python 任务/XX-xxx.py` 即可运行。
3. **去重合并算法** — 避免同一目标被多个模板匹配到多次，按 Y→X 排序实现类人眼扫描顺序。
4. **排除模板机制** — 02-浇水支持"排除模板"，将已浇过的花截图放入 `浇水排除/` 文件夹，自动跳过。
5. **组合脚本** — `09-种花并浇水.py` 将两个操作串联为流水线。
6. **拼音首字母过滤** — 种花选择时的智能过滤，输入 `xrk` 即可找到"向日葵"，无需面对海量花种盲目翻找。
7. **异步日志可选** — 日志模块设计为可切换同步/异步模式，性能敏感场景可开启异步避免 I/O 阻塞。

---

## 九、运行方式

```bash
# 安装依赖（项目根目录）
pip install -r requirements.txt

# 运行任意任务（项目根目录）
python 任务/01-种花.py
python 任务/02-浇水.py
# ... 以此类推

# 随时按 Esc 安全停止
```

## 十、局限与注意事项

1. **分辨率依赖** — 模板匹配基于像素截图，不同屏幕分辨率/游戏窗口大小需要重新截图素材。
2. **游戏界面稳定** — 脚本依赖游戏 UI 固定布局，若游戏版本更新导致界面变化需要更新模板。
3. **无游戏 API** — 纯视觉方案，无法获知游戏内部状态（如背包数量、冷却时间等）。
4. **单显示器** — 目前仅支持主显示器（`monitors[1]`），不支持多显示器环境。
5. **单人操作** — 脚本运行时鼠标被占用，无法同时进行其他操作。
